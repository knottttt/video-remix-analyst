# 使用说明

## Beat Sheet Document

1. 打开 `prompt.md`。
2. 将 `refs/` 里的参考图和 `prompt.md` 一起发给 ChatGPT 或你的图像生成调用。
3. 一次生成单页 production beat sheet，不要按多页 storyboard pack 处理。
4. 如果结果退化成海报、拼贴或单张主视觉，优先检查页面分块是否被模型忽略。
5. 保留 `manifest.json` 作为机器可读的输出契约。
